# DotOK Gap Snapshot — 2026-05-18 12:43:54

FOCUS_ROOT: `/Users/tom/Developer/pad`  
OK_DIR: `/Users/tom/Developer/pad/.ok`  
TASKS_DIR: `/Users/tom/Developer/pad/.tasks`

## OK sources read

- `.ok/macos.ok.canvas`

No first-class Markdown OK sources (`.ok/*.ok.md`) or root canvas source (`.ok.canvas`) were present.

## Canvas intent hypotheses

- `macos.ok.canvas#macos-app-ok` contains the macOS app ideal state.
- `macos.ok.canvas#application-menus` asserts that the native macOS app exposes platform menu-bar behavior.
- `macos.ok.canvas#standard-top-level-menus` means the main menu should expose standard top-level menus: File, Edit, View, Window, Help.
- `macos.ok.canvas#file-menu-new-canvas` means the File menu must include a `New Canvas` item.
- `macos.ok.canvas#new-canvas-command-n` means `New Canvas` must use the standard Command-N shortcut.
- `macos.ok.canvas#standard-shortcuts-preserved` plus `#tl-c2hhcGU6S05hbnZ3WlBDdGZCQmdNR2RfaEJK` means adding these menus must preserve expected native shortcuts for standard menu items rather than replacing AppKit conventions with a minimal custom menu.

These interpretations are low-ambiguity because node text and edge labels (`contains`, `asserts`, `has shortcut`) point directly at menu requirements.

## Reality observations

- `macos/AppifyUI2026-canvas/Sources/Canvas/AppDelegate.swift` configures a custom `NSApp.mainMenu` in `configureMainMenu()`.
- Current `configureMainMenu()` creates an application menu containing `Quit Canvas` with `q`.
- Current `configureMainMenu()` creates a File menu containing only `Open...` with `o`.
- No Edit, View, Window, or Help top-level menus are configured in `AppDelegate.swift`.
- No `New Canvas` menu item is configured in the File menu.
- No Command-N shortcut is configured for a new canvas action.
- Existing UI smoke coverage checks for an application menu and File menu only; it does not assert standard top-level menus, File > New Canvas, or Command-N.
- SwiftPM currently has `CanvasCoreTests` only; app menu behavior is not obviously covered by a unit-testable menu builder seam.

## Gaps

### gap-macos-menu-top-level-001 — Standard top-level menus absent

Expected: native menu bar exposes File, Edit, View, Window, and Help.  
Observed: only app and File menus are explicitly configured.  
OK refs: `.ok/macos.ok.canvas#standard-top-level-menus`

### gap-macos-menu-new-canvas-001 — File > New Canvas absent

Expected: File menu contains `New Canvas`.  
Observed: File menu contains only `Open...`.  
OK refs: `.ok/macos.ok.canvas#file-menu-new-canvas`

### gap-macos-menu-command-n-001 — New Canvas Command-N absent

Expected: `New Canvas` is bound to Command-N.  
Observed: no `New Canvas` item and no `n` key equivalent for a new-canvas command.  
OK refs: `.ok/macos.ok.canvas#new-canvas-command-n`

### gap-macos-menu-shortcuts-001 — Native standard shortcuts not protected

Expected: standard menus preserve expected macOS-native keyboard shortcuts.  
Observed: current custom menu is minimal and lacks an evident test seam or UI smoke assertion for preserving standard menu items and shortcuts.  
OK refs: `.ok/macos.ok.canvas#standard-shortcuts-preserved`, `.ok/macos.ok.canvas#tl-c2hhcGU6S05hbnZ3WlBDdGZCQmdNR2RfaEJK`

## Recommended task slices

One active slice is needed: reconcile macOS native application menus through tests-first menu coverage, implementation, and smoke verification.
