# DotOK gaps snapshot — 2026-05-18 13:25:24 -0400

- Focus root: `/Users/tom/Developer/pad`
- OK_DIR: `/Users/tom/Developer/pad/.ok`
- TASKS_DIR: `/Users/tom/Developer/pad/.tasks`

## First-class OK sources observed

- Markdown OK sources: none found in `.ok/*.ok.md`.
- Canvas OK sources:
  - `.ok/macos.ok.canvas`
  - `.ok/style.ok.canvas`
- Root canvas OK source: none found at `.ok.canvas`.

## Reality checks performed

- Read `.ok/style.ok.canvas` and `.ok/macos.ok.canvas` as coequal evergreen authority.
- Read `/Users/tom/Developer/work/daily/2026/05/style.canvas` as the user-reported regression fixture.
- Read `canvas.tldraw.adapter.js`, `pad.shebang.test.ts`, `macos/AppifyUI2026-canvas/Sources/Canvas/AppDelegate.swift`, `macos/AppifyUI2026-canvas/Sources/Canvas/DocumentWindowController.swift`, and existing macOS tests.
- Ran `bun test`: 51 pass, 0 fail.
- Ran `swift test` in `macos/AppifyUI2026-canvas`: 12 pass, 0 fail.
- Projected the external `style.canvas` fixture through `jsonCanvasToTldraw`; observed incorrect color and text-style projection despite passing tests.

## Canvas intent hypotheses

- `.ok/style.ok.canvas`: The graph roots at `style`, then splits into `text nodes` and `colors`. The least-unfit interpretation is that JSON Canvas imports into tldraw must render text nodes with a sans font, left horizontal alignment, and top vertical alignment; color handling must follow the JSON Canvas spec exactly for preset strings `"1"` through `"6"`, preserve hex colors as exact hues, avoid palette-index shifts, and keep node and edge colors in parity.
- `.ok/macos.ok.canvas`: The graph roots at `macos-app-ok` and expresses native macOS app invariants. The least-unfit interpretation is that menus, window closing, and WebView keyboard behavior are all durable app requirements. `Command-N` must create a new `.canvas` file and open it, `Command-W` and the red close button should close the current window immediately, and tldraw-handled keyboard interactions inside the WKWebView should not create macOS invalid-input/error sounds. Edge labels such as `contains`, `expects`, `asserts`, and `example` were interpreted as authored semantic hints, not a controlled protocol vocabulary.

## Gaps

### gap-style-color-mapping-001 — JSON Canvas color presets and hex hues project incorrectly

- Status: FALSE against `.ok/style.ok.canvas#colors-match-json-canvas-spec`, `.ok/style.ok.canvas#color-1-red`, `.ok/style.ok.canvas#color-2-orange`, `.ok/style.ok.canvas#color-3-yellow-brown`, `.ok/style.ok.canvas#color-4-green`, `.ok/style.ok.canvas#color-5-cyan`, `.ok/style.ok.canvas#color-6-purple-burple`, and `.ok/style.ok.canvas#hex-colors-preserve-hue`.
- Evidence: `canvas.tldraw.adapter.js` maps `3 → green`, `4 → blue`, `5 → violet`, and `6 → red`; non-preset hex colors fall through to `black`.
- Fixture evidence: projecting `/Users/tom/Developer/work/daily/2026/05/style.canvas` yielded `yellow/brown: green`, `green: blue`, `cyan: violet`, `burple: red`, and `#0000ff` blue as `black`.
- Impact: The user-visible canvas colors are shifted from the JSON Canvas source, matching the screenshot regression.

### gap-style-text-node-presentation-001 — Text node style does not match OK

- Status: FALSE against `.ok/style.ok.canvas#text-nodes-use-sans-font`, `.ok/style.ok.canvas#text-align-left`, and `.ok/style.ok.canvas#text-align-top`.
- Evidence: `nodeShapeForNode` sets `font: "draw"`, `align: "middle"`, and `verticalAlign: "middle"` for imported nodes.
- Fixture evidence: projected fixture rows report `draw:middle:middle` for every node.
- Impact: Loaded text nodes render with a hand-drawn centered appearance instead of sans, left-aligned, top-aligned labels.

### gap-style-node-edge-color-parity-001 — Node/edge color source parity is underspecified by tests

- Status: NOT PROVEN against `.ok/style.ok.canvas#node-edge-color-parity` and `.ok/style.ok.canvas#no-color-index-shift`.
- Evidence: Existing Bun tests cover round-tripping color fields but do not assert the tldraw projection color for every preset, default/no-color, and hex edge/node path.
- Impact: The current passing suite permits palette shifts and hex fallback regressions.

### gap-macos-new-canvas-flow-001 — Command-N item is wired but creation flow is absent

- Status: FALSE against `.ok/macos.ok.canvas#tl-c2hhcGU6Q3dfX1lURW8za1ExcU95M2JTak1N` and partially false against `.ok/macos.ok.canvas#file-menu-new-canvas` / `#new-canvas-command-n`.
- Evidence: `AppDelegate.newCanvasFromMenu(_:)` contains only `// Creation flow is not implemented yet; keep the native menu item wired.`
- Satisfied portion: File menu contains `New Canvas` and `keyEquivalent: "n"`.
- Impact: Command-N exposes the native affordance but does not create and open a new `.canvas` file as required.

### gap-macos-command-w-close-001 — Command-W close behavior is not explicitly implemented or tested

- Status: NOT PROVEN against `.ok/macos.ok.canvas#tl-c2hhcGU6T1U4S1VBbFNfRnhqTkI4QnhNdUct`.
- Evidence: `configureMainMenu()` does not define a `Close` menu item with key equivalent `w`, and tests do not assert Command-W behavior.
- Related satisfied portion: `DocumentWindowController` observes `NSWindow.willCloseNotification` and tears down the runner when the window closes.
- Impact: The app may rely on AppKit defaults rather than a deliberate native close command, leaving the OK requirement unguarded.

### gap-macos-webview-keyboard-quiet-001 — tldraw-handled keyboard events are not protected from macOS error sounds

- Status: NOT PROVEN against `.ok/macos.ok.canvas#webview-keyboard-events-supported`, `#hold-spacebar-no-error-sounds`, and `#keyboard-interactions-without-beeps`.
- Evidence: `DocumentWindowController.loadWebView(url:)` creates a plain `WKWebView`; there is no source or test seam for suppressing invalid-input beeps when the web app handles tldraw keyboard interactions such as held Spacebar.
- Impact: The OK source names the exact user-visible symptom, but the current test suite cannot detect or prevent it.

## Satisfied or currently covered expectations

- `.ok/macos.ok.canvas#standard-top-level-menus`: AppDelegate declares File, Edit, View, Window, and Help menus; Swift tests pass.
- `.ok/macos.ok.canvas#file-menu-new-canvas`: The File menu contains a `New Canvas` item.
- `.ok/macos.ok.canvas#new-canvas-command-n`: The New Canvas item uses key equivalent `n`.
- `.ok/macos.ok.canvas#standard-shortcuts-preserved`: Existing tests cover Undo, Cut, Copy, Paste, Select All, and Minimize shortcuts, but the command-W gap remains uncovered.
- `.ok/macos.ok.canvas#tl-c2hhcGU6ZlhxcVJTejJCTmQ4MjllQTJEcXlE` and `#tl-c2hhcGU6RGRhN2pFa3FyRmJBRHpFalBqbkpl`: the red window close path appears intentionally handled through `NSWindow.willCloseNotification` teardown.

## Human blockers

- No human input/access blocker is required for the fresh hierarchy. Canvas intent was interpretable enough to proceed without a clarification task.
